#!env bash
DIR=/usr/local/bin
ln -f -s ~/Setup/kubernetes/ks ${DIR}/ks 
ln -f -s ~/Setup/kubernetes/kgp ${DIR}/kgp 
ln -f -s ~/Setup/kubernetes/kl ${DIR}/kl
ln -f -s ~/Setup/kubernetes/klt ${DIR}/klt
