return {
  "chentoast/marks.nvim",
  opts = {
    -- Default keys to toggle signs
    default_mappings = true,
    -- Which marks to show
    signs = true,
    -- Persist marks across sessions
    force_write_shada = true,
  },
}
